# ServiceAccount whose JWT is exchanged for a Vault token (via the
# kubernetes auth method). The binding from this SA → Vault role
# `devops-backend` is configured in k8s/vault/install.ps1.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: backend
  namespace: devops-app
