# Repository credentials for ArgoCD (private GitLab repo).
# Pick ONE of the two options below and delete the other.
#
# ---- Option A: HTTPS + Personal/Project Access Token ----
apiVersion: v1
kind: Secret
metadata:
  name: gitops-repo
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  type: git
  # TODO: replace with your actual repo URL
  url: http://gitlab.dwl.internal/infrastructure/gitops/kustomize-demo.git
  # GitLab Project/Group/Personal Access Token with `read_repository` scope.
  # Recommend a deploy token scoped to this repo only.
  username: argocd-sync-repo
  password: glpat-B6RcsW5zmMxhLS0AEVNcI286MQp1Om4H.01.0w1f56zrc
---
# ---- Option B: SSH key (delete Option A above if using this) ----
# apiVersion: v1
# kind: Secret
# metadata:
#   name: gitops-repo
#   namespace: argocd
#   labels:
#     argocd.argoproj.io/secret-type: repository
# stringData:
#   type: git
#   url: git@gitlab.example.com:infrastructure/gitops.git
#   sshPrivateKey: |
#     -----BEGIN OPENSSH PRIVATE KEY-----
#     <paste deploy key private content here>
#     -----END OPENSSH PRIVATE KEY-----
